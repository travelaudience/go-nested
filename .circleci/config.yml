version: 2.1

orbs:
  ta-go: travelaudience/go@0.9

executors:
  golang-executor:
    docker:
    - image: cimg/go:1.25

workflows:
  build_and_test:
    # CircleCI is intentionally disabled after migrating this repository to
    # GitHub Actions. The active workflows are under .github/workflows/.
    when: false
    jobs:

    - ta-go/checks:
        name: check
        exec: golang-executor
        run-static-analysis: true

    - ta-go/test_and_coverage:
        name: test
        exec: golang-executor
